import { buildAPI, exposeExpressAPI, generateServerSchema } from "./nodes/server";
import { generateClientSchema, generateClientSchemaFromJson, generators } from "./nodes/client";
 
const rpcSync = {
    server: {
        buildAPI,
        generateServerSchema,
        exposeExpressAPI
    },
    client: {
        generateClientSchema,
        generateClientSchemaFromJson
    },
    generators,
};


export default rpcSync